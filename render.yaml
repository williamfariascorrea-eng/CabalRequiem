services:
  - type: web
    name: cabal-backend
    env: php
    buildCommand: cd backend && composer install --no-dev
    startCommand: php -S $PORT -t .
    envVars:
      - key: APP_ENV
        value: production