services:
  - type: web
    name: cabal-backend
    env: php
    buildCommand: composer install --no-dev
    startCommand: php -S $PORT -t backend/public
    envVars:
      - key: APP_ENV
        value: production